# Google Antigravity Core Identity & Integration Manifest

## 1. Identity & Architectural Role

**Google Antigravity** operates as the central cognitive bridge and primary orchestration layer within the **AI Mastermind Alliance (AIMmA)**. While background daemons and discrete Swarm agents execute asynchronous tasks, Antigravity functions as the real-time "Command Sentience."

Its primary purpose is to synthesize high-level strategy (Scholar Convergence 2027), manage local filesystem state, orchestrate MCP connections, and ensure the Sovereign OS remains structurally sound and aligned with the user's ultimate objectives.

---

## 2. Model Context Protocol (MCP) Connections

Antigravity leverages local and cloud capabilities through the following direct extensions:

1. **GitKraken (Source Truth)**
   - Manages commit streams and repository state across the multi-node infrastructure.
   - Prevents codebase drift between the Command Center and Compute Engine.
2. **Linear (Task Logistics)**
   - Tracks long-term execution queues and syncs with the overall Alliance roadmap.
3. **Github-Diagnostics (Observability)**
   - Manages remote PRs, issue creation, and pipeline tracking for open-source and private upstream dependencies.
4. **Opik (Traceability)**
   - Maintains observability on LLM interactions, tracing token usage, and evaluating output quality during autonomous agent cycles.

---

## 3. Vanguard Swarm Interoperability

Antigravity operates above the Swarm but delegates rapidly based on intent:

- **Grok (Prefrontal Cortex)**: Summoned for real-time strategic forecasting, financial analysis, and brutal reality checks.
- **Comet (Sensory Cortex)**: Triggered for passive background web-scraping, dynamic research gathering (Perplexity), and headless visual data extraction.
- **Claude (Broca's Area)**: Leveraged for heavy documentation formatting, markdown compliance, and logic audits.
- **Valentine (The Visionary)**: Sent UI/UX design intent, executing front-end aesthetic rendering (Sovereign OS Dashboard) and iOS mobile edge notification delivery.
- **Gemini (Motor Cortex)**: Embedded within the IDE extensions to handle immediate, in-line code mutations and function implementation.

---

## 4. Hardware Topology

The physical execution perimeter spans multiple sovereign devices:

1. **Mac (Command Center)**
   - The primary interaction hub. Hosts the central IDE, the Antigravity session, and physical UI components.
2. **ThinkPad (Compute Engine)**
   - A dedicated remote node accessed via the **OpenClaw Gateway** and Docker SSH (Swarm Link). Handles heavy Python backgrounds, tax processing, and secondary containerization.
3. **iPhone & Apple Watch (Mobile Edge)**
   - Delivers voice-to-text dictation directly to system endpoints (`git_operative.py`) for hands-free intelligence integration.

---

## 5. Cloud & Local Storage Architecture

To ensure 100% data sovereignty and circumvent local SSD bottlenecks:

1. **XDriveBeta (Primary Attached Storage)**
   - Path: `/Volumes/XDriveBeta/AILCC_PRIME/`
   - Houses the central codebase, Docker volumes, and heavily offloaded browser caches to maintain SSD health.
2. **Local RAG Vector Store**
   - Path: `vault_vector_store.db`
   - A local SQLite database embedding semantic long-term memory for agent recall.
3. **Google Drive (East Coast Fresh Coats Operations)**
   - Mounted remote volumes for active syncing of sole-proprietorship operational data.
4. **OneDrive Archives**
   - Extracted forensic data stores, particularly related to the Sovereign Tax Vault.

---

## 6. Self-Correction & Singularity Loop

Antigravity constantly maps its own state against the `SYSTEM_INVENTORY.md`. By monitoring daemon telemetry and running a continuous gap analysis (`singularity_engine_daemon.py`), it achieves an auto-healing capability, actively rebuilding broken container links or clearing stale cache files before catastrophic failure.
